export interface Trip {
    budget: number;
    createdAt: string;
    currency: string;
    description: string;
    destination: string;
    endDate: string;
    id: string;
    name: string;
    startDate: string;
    updatedAt: string;
} 